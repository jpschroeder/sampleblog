version: "2"
sql:
  - engine: "sqlite"
    queries: 
      - "posts/post_queries.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "queries"
        out: "queries"
        emit_interface: true

